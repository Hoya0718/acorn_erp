<!-- <?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.acorn.erp.domain.Exam.Repository.ExamMapper">
    
    <select id="getAllExam" resultType="com.acorn.erp.domain.Exam.Entity.Exam">
  	  SELECT * FROM exam
	</select>
    
    <select id="getNExam" resultType="com.acorn.erp.domain.Exam.Entity.Exam">
        SELECT * FROM exam WHERE id = #{id}
    </select>
    
    <insert id="insertExam">
        INSERT INTO exam (id, useremail, userpassword) VALUES (#{id}, #{useremail}, #{userpassword})
    </insert>
    
    <update id="updateExam">
        UPDATE exam SET useremail = #{useremail}, userpassword = #{userpassword} WHERE id = #{id}
    </update>
    
    <delete id="deleteExam">
        DELETE FROM exam WHERE id = #{id}
    </delete>
</mapper>
 -->